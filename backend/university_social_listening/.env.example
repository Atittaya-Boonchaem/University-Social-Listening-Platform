# Backend-side path to the fine-tuned model. Relative paths are resolved from
# the backend working directory; use an absolute path in production if needed.
WANGCHAN_MODEL_DIR=./models/finetuned_model
